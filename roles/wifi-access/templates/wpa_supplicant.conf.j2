# {{ ansible_managed }}
country=DE
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
update_config=1

network={
        ssid="{{ wifi_acceess_wifi_ssid }}"
        psk="{{ wifi_acceess_wifi_psk }}"
        key_mgmt={{ wifi_acceess_wifi_key_mgmt }}
}